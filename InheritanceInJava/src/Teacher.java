public class Teacher extends Person{
 public Teacher(String name,String surname, int age ){
     super(name,surname,age);

    // Inheritance means "Is a connection" eg. here Student and teacher both are person.
     // Here we created person class which is our parent class or  we can say it is base class.
 }
}
